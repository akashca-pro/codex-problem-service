/**
 * Enum representing error types related to user authentication.
 *
 * @enum
 */
export enum SystemErrorType { 
    /**
     * Error type indicating that the application encounter internal error.
     */
    InternalServerError = 'Internal Server Error',

}