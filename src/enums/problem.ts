/**
 * Enum representing the available languages for code execution.
 * 
 * @enum
 */
enum Language {
    JAVASCRIPT = 'javascript',
    PYTHON = 'python',
    GO = 'go'
}

/**
 * Enum representing the difficulty of the problem.
 * 
 * @enum
 */
enum Difficulty {
    EASY = 'easy',
    MEDIUM = 'medium',
    HARD = 'hard'
}